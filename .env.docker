# Variáveis de ambiente para o Docker Compose
# Copie este arquivo para .env se quiser sobrescrever valores

# Banco de dados
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=alloc-platform

# Backend
BACKEND_PORT=3001

# Frontend
FRONTEND_PORT=3000
