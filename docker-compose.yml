services:
  postgres:
    image: postgres:16-alpine
    container_name: alloc-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: alloc_platform
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
